/*import React from 'react';
import {Grid} from '@mui/material';
import {ClientTable} from '../../src/components';

const SearchClient = () => {
	return (
		<Grid container spacing={0}>
			<Grid item xs={12} lg={12}>
				<ClientTable title="Table of users" />
			</Grid>
		</Grid>
	);
};

export default SearchClient;
*/